name: Build and push custom image

on:
  push:
    branches: [develop]
  workflow_dispatch: {}

env:
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/label-studio-cracked

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    # Cold build of the full multi-stage image takes ~50min; the cap must exceed
    # that so at least one run finishes and populates the gha cache.
    timeout-minutes: 60
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v6

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v6
        id: meta
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=sha,format=long
            type=raw,value=latest,enable={{is_default_branch}}

      - uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          # mode=max caches intermediate stages (e.g. the frontend build) so
          # subsequent builds reuse them instead of rebuilding from scratch.
          cache-to: type=gha,mode=max
